# Archive

These files are historical working notes and session plans.

They are preserved for context but are not part of the active read-first path.

Use the active docs in the parent `docs/` folder first.
